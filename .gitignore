# Node / Next.js
node_modules/
.next/
out/
dist/
build/

# Secrets / env (NEVER commit) — keep only the .example template
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/

# Pipeline data & outputs (regenerated)
pipeline/data/
pipeline/output/
pipeline/cache/
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local deploy secrets — never commit
DEPLOY_ENV_VARS.txt
